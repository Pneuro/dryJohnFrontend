export default function SectionSeparator() {
  return (
    <hr style={styles.separator} className="border-accent-2 mt-28 mb-24" />
  );
}
const styles = {
  separator: {
    marginTop: "100px",
    borderColor: "#00aeee",
    marginBottom: "100px",
  },
};
